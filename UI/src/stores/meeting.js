
import { writable } from "svelte/store";

export const meetingId = writable(null);
export const participantId = writable(null);
export const status = writable(null);
export const closingTime = writable(null);
export const roomId = writable(null);
export const token = writable(null);
export const partnerId = writable(null);